name: custom_tabbar_indicator
description: Flutter Custom TabBar Indicator
version: 1.0.0
author: salihcanbinboga
homepage:

environment:
  sdk: ">=2.7.0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter: